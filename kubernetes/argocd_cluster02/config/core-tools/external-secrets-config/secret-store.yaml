apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: vault
  namespace: security
spec:
  provider:
    vault:
      server: "https://vault.dublinconsulting.com.br"
      path: "secret"
      version: "v2"
      auth:
        tokenSecretRef:
          name: vault-token
          key: token
          namespace: security
#comments add https

### change path to cubbyhole/